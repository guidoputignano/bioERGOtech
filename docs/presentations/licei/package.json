{
  "name": "licei-rete-di-scopo-deck",
  "private": true,
  "description": "Builds the schools deck PDF from the self-contained HTML source.",
  "scripts": {
    "build": "node build-pdf.mjs",
    "proof": "node build-pdf.mjs --shots"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0"
  }
}
